# you will need a script to setup the (secret) ENV vars to run the backend
# Gitlab CI will inject the following secret ENV variables, you must do as well
#
#
# create a script which has the following prepared:
set JAVA_TEST_PRIVATE_TOKEN=#your secret gitlab api token
set DB_HOST=localhost
set DB_PORT=6000
set DB_NAME=mlreef_backend
set DB_USER=mlreef
set DB_PASSWORD=password
